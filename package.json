{
  "name": "discord-mobile-identify-patcher",
  "version": "0.1.0",
  "description": "Independent Discord Mobile IDENTIFY patcher.",
  "bin": {
    "discord-mobile-identify-patcher": "src/cli.mjs"
  },
  "scripts": {
    "test": "node --test",
    "check": "npm test",
    "install:win": "powershell -ExecutionPolicy Bypass -File scripts/install-windows.ps1",
    "install:mac": "bash scripts/install-macos.sh"
  },
  "dependencies": {
    "@electron/asar": "^3.4.1"
  },
  "devDependencies": {}
}
